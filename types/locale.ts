export interface LocaleMessages {
	login: {
		title: string
		user_login_tip: string
		no_entry: string
		form: {
			validate: {
				email: string
				mobile: string
			}
		}
	}
	layout: {
		logout: string
		account: string
		menu: string
	}
}
