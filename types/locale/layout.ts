export default interface Layout {
	logout: string
	avatar: {
		reset: string
	}
	setting: {
		title: string
		update_menu: {
			title: string
			desc: string
			btn_text: string
			feedback: string
		}
		language: {
			title: string
		}
		theme: {
			title: string
		}
	}
}
