export default interface Layout {
	not_found: string
	logout: string
	auth: {
		loading: string
	}
	menu: {
		search_placeholder: string
	}
	avatar: {
		reset: string
	}
	setting: {
		language: {
			title: string
		}
		theme: {
			title: string
		}
		system: {
			name: string
			version: string
		}
	}
}
